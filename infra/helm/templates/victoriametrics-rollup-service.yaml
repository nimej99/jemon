apiVersion: v1
kind: Service
metadata:
  name: {{ include "jemon.fullname" . }}-vm-rollup
  labels:
    {{- include "jemon.labels" . | nindent 4 }}
    app.kubernetes.io/component: victoriametrics-rollup
spec:
  type: {{ .Values.victoriametricsRollup.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "jemon.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: victoriametrics-rollup
  ports:
    - name: http
      port: {{ .Values.victoriametricsRollup.service.port }}
      targetPort: http
      protocol: TCP
